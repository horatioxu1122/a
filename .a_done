summary: cut tokens across a.c + all lib/*.c files
cuts: AB macro (15 sites), ajoin helper (15 sites), ssh_idx dedup (3 sites),
      perf_limit dedup, comment header cuts, dead forward decl removed,
      log sub-check merge, tmux comment block cut
test: cd ~/a && bash a.c && adata/local/a help && adata/local/a hi && adata/local/a diff
